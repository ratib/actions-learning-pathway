name: hello-world
on:
  workflow_dispatch:

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout sourcecode
        uses: actions/checkout@v6
      - run: echo "$(cat hello_world.txt)"
